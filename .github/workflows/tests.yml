name: Tests

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # Step 1: Code Quality (runs first)
  code-quality:
    name: Code Quality
    runs-on: macos-26

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install SwiftLint
        run: brew install swiftlint

      - name: Run SwiftLint
        run: |
          echo "🔍 Running SwiftLint with strict mode..."
          swiftlint lint --strict
          echo "✅ SwiftLint passed" >> $GITHUB_STEP_SUMMARY

      - name: Verify Platform Configuration
        run: |
          echo "🔍 Verifying Package.swift platform configuration..."
          # Check for macOS < 26 (not allowed)
          if grep -E "platforms.*\.macOS\(\.v([0-9]|1[0-9]|2[0-5])\)" Package.swift; then
            echo "❌ Package.swift contains macOS version < 26"
            exit 1
          fi
          # Check for iOS < 17 (not allowed)
          if grep -E "platforms.*\.iOS\(\.v([0-9]|1[0-6])\)" Package.swift; then
            echo "❌ Package.swift contains iOS version < 17"
            exit 1
          fi
          echo "✅ Platform configuration verified (macOS 26+, iOS 17+)" >> $GITHUB_STEP_SUMMARY

      - name: Check for TODOs in committed code
        shell: bash {0}
        run: |
          if git diff origin/main...HEAD 2>/dev/null | grep -E "^\+.*TODO|^\+.*FIXME"; then
            echo "⚠️  Warning: New TODOs or FIXMEs detected"
          fi

      - name: Check file sizes
        shell: bash {0}
        run: |
          find Sources -type f -name "*.swift" -size +100k -exec echo "⚠️  Large file: {}" \;

      - name: Check for print statements
        shell: bash {0}
        run: |
          if grep -r "print(" Sources/ --include="*.swift" | grep -v "// print"; then
            echo "⚠️  Warning: print() statements found in source code"
          fi

      - name: Swift build check
        run: |
          echo "🔨 Verifying Swift package builds..."
          swift build --configuration debug
          echo "✅ Swift package builds successfully" >> $GITHUB_STEP_SUMMARY

  # Step 2: Unit Tests (runs after code quality passes)
  unit-tests-macos:
    name: macOS Unit Tests
    runs-on: macos-26
    needs: [code-quality]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Show Swift version
        run: swift --version

      - name: Build for macOS
        run: |
          echo "🔨 Building for macOS"
          swift build

      - name: Run FCPXML DTD validation tests
        run: |
          echo "🔍 Running FCPXML DTD validation tests..."
          swift test --filter FCPXMLDTDValidation 2>&1 | tee dtd_validation_output.txt

          if grep -q "Test run with .* tests in .* suites passed" dtd_validation_output.txt; then
            echo "✅ All FCPXML DTD validation tests passed" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "### DTD Validation Results" >> $GITHUB_STEP_SUMMARY
            echo "- Validates generated FCPXML against official DTD files (v1.9-v1.13)" >> $GITHUB_STEP_SUMMARY
            echo "- Ensures compliance with Apple's FCPXML specification" >> $GITHUB_STEP_SUMMARY
            echo "- Tests empty timelines, single/multi-clip, multi-lane, and various video formats" >> $GITHUB_STEP_SUMMARY
          else
            echo "❌ FCPXML DTD validation failed" >> $GITHUB_STEP_SUMMARY
            exit 1
          fi

      - name: Run all unit tests on macOS
        run: |
          echo "🧪 Running all unit tests on macOS"
          swift test --parallel 2>&1 | tee test_output_macos.txt

          if grep -q "Test run with .* tests in .* suites passed" test_output_macos.txt; then
            echo "✅ All unit tests passed" >> $GITHUB_STEP_SUMMARY
            TEST_COUNT=$(grep -o "[0-9]* tests" test_output_macos.txt | tail -1 | grep -o "[0-9]*")
            echo "- Executed $TEST_COUNT tests successfully" >> $GITHUB_STEP_SUMMARY
          else
            echo "❌ Unit tests failed" >> $GITHUB_STEP_SUMMARY
            exit 1
          fi
